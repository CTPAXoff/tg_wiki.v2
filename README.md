📘 Минималистичное ТЗ на разработку ядра бэкенда и микро-фронта Telethon-парсера
0. Краткая цель проекта

Создать локальное приложение (Win11), которое:

Позволяет пользователю авторизоваться в Telegram по номеру телефона.

Надёжно хранит и восстанавливает сессию (StringSession).

При падении сессии позволяет легко сбросить и повторно пройти авторизацию.

Позволяет выбрать чат (или несколько) и спарсить текстовые сообщения за нужный период.

Записывает всё в локальную БД, не сталкиваясь с блокировками и not responding ситуациями.

Обладает простейшим фронтом.

1. Архитектура (верхнеуровневая)
Backend

FastAPI

Telethon

SQLite в режиме WAL

SQLAlchemy (async)

NullPool

AES-шифрование StringSession

Логи только ERROR и WARNING в файл

Frontend

React 19 + Vite

Минимальный интерфейс:

Ввод номера телефона

Ввод кода

Кнопка "Сбросить сессию"

Кнопка "Запросить список чатов"

Выбор чата + периода

Кнопка "Старт парсинга"

Прогресс-индикатор

2. Telegram авторизация
2.1 Формат сессии

Используем Telethon StringSession (строка).

Причины:

Не ломается при параллельных операциях.

Не использует внутренний SQLite Telethon-а.

Легко хранить/очищать/шифровать.

Мгновенно пересоздаётся при падении.

2.2 Базовый поток (flow)
POST /auth/request-code

Вход:

phone (string)

Действия:

Инициализировать Telethon клиент (без сессии).

Выполнить send_code_request(phone).

Сохранить phone_code_hash в таблицу sessions.

Вернуть frontend статус OK.

POST /auth/confirm-code

Вход:

phone

code (из Telegram)

Действия:

Взять phone_code_hash из БД.

Вызвать client.sign_in(phone, code, phone_code_hash).

Получить StringSession.

Зашифровать AES-ключом из .env.

Сохранить в БД (заменяя старую).

Выход:
{ status: "ok" }

GET /auth/status

Проверяет:

есть ли строка сессии

может ли Telethon выполнить GetMe()

если ошибка → status = invalid

POST /auth/reset

Сбрасывает:

string_session = NULL

phone_code_hash = NULL

status = "empty"

session_expires = NULL

Фронт переводит пользователя на ввод номера.

3. Сценарий падения сессии
3.1 Причины падения

Захватываем следующие ошибки:

AuthKeyError

SessionPasswordNeededError

rpc_errors.BadRequestError: USER_DEACTIVATED

AuthKeyUnregisteredError

ValueError: A wait of x seconds is required (FloodWait)

ConnectionError

«Used from different IP»

3.2 Реакция back-end

При любой ошибке авторизации:
→ помечаем sessions.status = "invalid"
→ фронт в следующем запросе видит статус invalid
→ показывает кнопку «Переподключить»

3.3 Реакция front-end

Показывает окно:

Сессия недействительна.
[ Перезапустить авторизацию ]
[ Сбросить сессию ]


Кнопки вызывают соответствующие API.

4. Работа с БД
4.1 Требования

1 пользователь

несколько параллельных процессов (потенциально)

SQLite must NEVER be locked

переносимость на сервер в будущем

4.2 Настройки

SQLite → WAL режим

SQLAlchemy → Async + NullPool

PRAGMA busy_timeout = 5000

Retry обёртка с exponential backoff для всех записьных операций

5. Структура БД
5.1 Таблица sessions (1 запись)
Поле	Тип	Описание
id	int	всегда = 1
phone	string	номер телефона
string_session	string	AES-шифрованная строка
phone_code_hash	string	хэш последнего запроса
status	enum("empty","valid","invalid")	статус
created_at	datetime	
updated_at	datetime	
5.2 Таблица messages
Поле	Тип
id	integer primary key
chat_id	int64
msg_id	int64
sender_id	int64
sender_name	string
text	string
date	datetime
is_reply	bool
reply_to_msg_id	int64 / null
entities	json / null
raw_json	json
5.3 Индексы

index(chat_id, msg_id)

index(sender_id)

index(date)

6. Парсинг чата
6.1 API
GET /telegram/chats

Получить список чатов (client.get_dialogs()).

POST /telegram/fetch-messages

Вход:

chat_id

from_date (optional)

to_date (optional)

Выполняет:

пакетный импорт (100–200 сообщений за раз)

только text-сообщения

JSON-сущности → entities

reply-chain через reply_to

Важно:
Все записи проходят через retry с exponential backoff.

6.2 Ограничения

Игнорируем: gif, video, photo, voice, document, service messages.

Только текст, ссылки, упоминания.

raw_json сохраняем на всякий случай.

7. Фронтенд (минимальный)
Страницы:

Логин

поле номера

кнопка "Отправить код"

Подтверждение

поле для кода

кнопка "Подтвердить"

Статус авторизации

label: valid / invalid

кнопка "Сбросить сессию"

кнопка "Переподключить"

Выбор чата

список чатов

поле "Дата с"

поле "Дата по"

кнопка "Старт"

Прогресс парсинга

% прогресса

количество записанных сообщений

8. Обработка ошибок

Единая структура:

{
  "error": true,
  "type": "SessionInvalid",
  "message": "Telegram session invalid, please re-auth."
}


Типы ошибок:

SessionInvalid

CodeExpired

DatabaseLockRetryFailed

TelegramFloodWait

TelegramConnectionLost

UnknownError

9. Логи

Обязательные (в файл):

ERROR (все)

WARNING (сетевые ошибки Telethon, БД, парсинга)

Запрещено:

DEBUG в продакшене

запись сообщений в текстовый лог

запись всего raw JSON потока в лог

10. Edge cases, которые обязан покрыть разработчик

Телефон введён неверно → корректный ответ.

Код просрочен → новый код.

Потеря соединения → retry + ошибку на фронт.

FloodWait → сообщить пользователю время ожидания.

Параллельный импорт двух чатов → запрещено (mutex).

SQLite locked → max 3 retry → ошибка.

Пользователь сбросил сессию в процессе парсинга → graceful shutdown.

Парсинг больших чатов с 200k+ сообщений → стабильная работа.

11. Готовый список задач для разработчика
Backend

 Настроить проект FastAPI (async)

 Подключить SQLAlchemy + SQLite + WAL + NullPool + busy_timeout

 Реализовать AES-шифрование StringSession

 Реализовать endpoints: request-code, confirm-code, status, reset

 Сделать менеджер Telethon клиента (lazy load + error handling)

 Обработку падения сессии

 Реализовать список чатов

 Авторский парсер сообщений (batch + retry)

 Запись сообщений в БД без блокировок

 Файл логов с WARNING/ERROR

Frontend

 Мини-UI для логина

 Мини-UI для списка чатов

 Контроль статуса авторизации

 Кнопки: "reset session", "restart auth"

 Прогресс бар парсинга

12. Конечный результат

После выполнения ТЗ разработчик должен получить систему, которая:

Не ломается.

Сессия восстанавливается одной кнопкой.

При любой ошибке не умирает, а сообщает о ней.

Не блокирует SQLite.

Парсит чаты быстро и надёжно.

Имеет аккуратный минимальный фронт.